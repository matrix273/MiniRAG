# ============ kb.example.com (PageIndex 项目) ============
server {
    listen 80;
    server_name kb.example.com;

    # 前端静态文件
    location / {
        root /path/to/PageIndex/frontend/dist;
        try_files $uri $uri/ /index.html;
        
        # 静态资源缓存
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 文件上传大小限制
        client_max_body_size 50M;
    }

    # 文件服务代理
    location /files/ {
        proxy_pass http://127.0.0.1:8000/files/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        
        # 静态文件缓存
        expires 1h;
        add_header Cache-Control "public, max-age=3600";
    }
}

# ============ hr.example.com (HR 项目) ============
server {
    listen 80;
    server_name hr.example.com;

    # 前端静态文件
    location / {
        root /path/to/hr/frontend/dist;
        try_files $uri $uri/ /index.html;
        
        # 静态资源缓存
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
        }
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:8001/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 文件上传大小限制
        client_max_body_size 50M;
    }
}

# ============ 默认服务器（可选）============
# 处理未匹配的域名请求
server {
    listen 80 default_server;
    server_name _;
    return 404;
}

# HTTPS 配置（可选，推荐生产环境使用）
# server {
#     listen 443 ssl http2;
#     server_name kb.example.com;
#
#     ssl_certificate /path/to/kb-cert.pem;
#     ssl_certificate_key /path/to/kb-key.pem;
#
#     # 复制上面 kb.example.com 的 location 配置到这里
# }
#
# server {
#     listen 443 ssl http2;
#     server_name hr.example.com;
#
#     ssl_certificate /path/to/hr-cert.pem;
#     ssl_certificate_key /path/to/hr-key.pem;
#
#     # 复制上面 hr.example.com 的 location 配置到这里
# }
